# TGTB

The benchmark adapter is in `attack.py`; `run.py` prepares and runs only TGTB.
Pinned upstream implementation files are in `upstream/`, with source revision
and file hashes in `UPSTREAM.json`.

See [baseline reproduction instructions](../README.md) for installation and
prepare/launch/status/summarize commands. Generated `runs/` data stays local.
